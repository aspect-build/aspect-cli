def _impl(ctx):
    print("I am a task that is not auto-discovered but manually registered in MODULE.aspect instead")
    return 0

user_task = task(
    name = "user-task",
    group = ["user", "elsewhere"],
    implementation = _impl,
    args = {},
)