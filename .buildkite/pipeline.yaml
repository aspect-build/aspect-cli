# Generated by: aspect workflows migrate --host=buildkite
# Source: .aspect/workflows/config_aws.yaml
# DO NOT EDIT - regenerate with 'aspect workflows migrate'

env:
  ASPECT_DEBUG: "1"
secrets:
  - ASPECT_API_TOKEN
steps:
  - key: __main__::warm
    label: ":aspect: Building CLI"
    agents:
      queue: aspect-huge
    timeout_in_minutes: 20
    retry:
      automatic:
        - exit_status: 78
          limit: 1
    command:
      - echo "Pre-command builds and uploads CLI to remote cache"
  - key: __main__::test
    depends_on:
      - __main__::warm
    label: ":aspect: Test"
    agents:
      queue: aspect-default
    timeout_in_minutes: 20
    retry:
      automatic:
        - exit_status: 78
          limit: 1
    command:
      - |
        echo "--- :bazel: aspect test //..."
        ASPECT_DEBUG=1 aspect test //... --bazel_flag=--build_tests_only
  - key: __main__::build
    depends_on:
      - __main__::warm
    label: ":aspect: Build"
    agents:
      queue: aspect-huge
    timeout_in_minutes: 20
    retry:
      automatic:
        - exit_status: 78
          limit: 1
    command:
      - |
        echo "--- :bazel: aspect build //:launcher //:cli"
        aspect build //:launcher //:cli
        # Use cquery to get the actual output paths (handles platform transitions on Linux)
        WORKSPACE_ROOT=$$(pwd)
        LAUNCHER=$$WORKSPACE_ROOT/$$(bazel $$BAZEL_STARTUP_OPTS cquery $$BAZEL_BUILD_OPTS --output=files //crates/aspect-launcher)
        CLI=$$WORKSPACE_ROOT/$$(bazel $$BAZEL_STARTUP_OPTS cquery $$BAZEL_BUILD_OPTS --output=files //crates/aspect-cli)
        echo "--- :aspect: aspect demo template-demo"
        $$LAUNCHER demo template-demo
        echo "--- :aspect: aspect user nested user_task"
        $$LAUNCHER user nested user_task
        echo "--- :aspect: aspect user nested user_task_reexport"
        $$LAUNCHER user nested user_task_reexport
        echo "--- :aspect: aspect user user-task-manual"
        $$LAUNCHER user user-task-manual
        echo "--- :aspect: aspect user user-task-added-by-config"
        $$LAUNCHER user user-task-added-by-config
        echo "--- :aspect: aspect user user-task-subdir (in crates/aspect-cli)"
        (
          cd crates/aspect-cli
          $$LAUNCHER user user-task-subdir
        )
        echo "--- :aspect: aspect help"
        $$LAUNCHER help
        echo "--- :aspect: aspect-launcher --version"
        $$LAUNCHER --version
        echo "--- :aspect: aspect-cli --version"
        $$CLI --version
        echo "--- :aspect: aspect version"
        $$LAUNCHER version
        echo "--- :aspect: aspect tests axl"
        # Run AXL tests last — cancel tests may kill the Bazel server,
        # which invalidates bazel-out paths used by $$LAUNCHER above.
        $$LAUNCHER tests axl
  - key: __main__::bazelrc-e2e
    depends_on:
      - __main__::warm
    label: ":bazel: BazelRC e2e (no server restart)"
    agents:
      queue: aspect-default
    timeout_in_minutes: 20
    retry:
      automatic:
        - exit_status: 78
          limit: 1
    command:
      - |
        echo "--- :bazel: Warm up Bazel server"
        bazel $$BAZEL_STARTUP_OPTS test $$BAZEL_BUILD_OPTS //crates/bazelrc:bazelrc_test
        OUTPUT_BASE=$$(bazel $$BAZEL_STARTUP_OPTS info output_base)
        SERVER_LOG=$$(bazel $$BAZEL_STARTUP_OPTS info server_log)
        BEFORE_PID=$$(bazel $$BAZEL_STARTUP_OPTS info server_pid)
        echo "output_base: $$OUTPUT_BASE"
        echo "server_log:  $$SERVER_LOG"
        echo "server_pid:  $$BEFORE_PID"
        echo "--- :bazel: bazel info (before aspect)"
        bazel $$BAZEL_STARTUP_OPTS info
        echo "--- :aspect: aspect test //crates/bazelrc:bazelrc_test"
        aspect test //crates/bazelrc:bazelrc_test
        AFTER_PID=$$(bazel $$BAZEL_STARTUP_OPTS info server_pid)
        echo "--- :bazel: bazel info (after aspect)"
        bazel $$BAZEL_STARTUP_OPTS info
        echo "--- :scroll: Server log"
        cat "$$SERVER_LOG" || true
        echo "--- :buildkite: Uploading artifacts from output_base"
        buildkite-agent artifact upload "$$SERVER_LOG"
        buildkite-agent artifact upload "$$OUTPUT_BASE/command.log"
        buildkite-agent artifact upload "$$OUTPUT_BASE/server/**/*"
        if [ "$$BEFORE_PID" != "$$AFTER_PID" ]; then
          echo "^^^ +++"
          echo "ERROR: Bazel server was restarted (PID $$BEFORE_PID → $$AFTER_PID)"
          echo "This means aspect produced different startup flags than bazel."
          exit 1
        fi
        echo "OK: server PID unchanged ($$BEFORE_PID)"
  - key: __main__::format
    label: ":broom: Format"
    depends_on:
      - __main__::warm
    agents:
      queue: aspect-default
    timeout_in_minutes: 20
    retry:
      automatic:
        - exit_status: 78
          limit: 1
    command:
      - |
        echo "--- :bazel: bazel run //tools/format:format.check"
        bazel $$BAZEL_STARTUP_OPTS run $$BAZEL_BUILD_OPTS //tools/format:format.check
  - key: __main__::delivery
    label: ":package: Delivery"
    depends_on:
      - __main__::warm
    agents:
      queue: aspect-default
    timeout_in_minutes: 20
    retry:
      automatic:
        - exit_status: 78
          limit: 1
    command:
      - aspect delivery --build_url $BUILDKITE_BUILD_URL --commit_sha $BUILDKITE_COMMIT --force_target //:hello -- //:hello //:hello2
