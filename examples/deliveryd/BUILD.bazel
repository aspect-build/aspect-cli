load("@rules_go//go:def.bzl", "go_binary", "go_library")

exports_files(["go.mod"])

go_library(
    name = "deliveryd_lib",
    srcs = [
        "handlers.go",
        "main.go",
        "redis.go",
        "server.go",
        "types.go",
    ],
    importpath = "github.com/aspect-build/deliveryd",
    deps = [
        "@com_github_redis_go_redis_v9//:go-redis",
    ],
)

go_binary(
    name = "deliveryd",
    embed = [":deliveryd_lib"],
    visibility = ["//visibility:public"],
)
