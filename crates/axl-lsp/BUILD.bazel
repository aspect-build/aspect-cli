load("//bazel/rust:defs.bzl", "rust_binary")

rust_binary(
    name = "axl-lsp",
    srcs = glob(["src/**/*.rs"]),
    proc_macro_deps = [
        "@crates//:starlark_derive",
    ],
    visibility = [
        "//crates/aspect-cli:__pkg__",
    ],
    deps = [
        "//crates/axl-runtime",
        "@crates//:anyhow",
        "@crates//:starlark",
        "@crates//:starlark_lsp",
    ],
)
