def _impl(ctx):
    print("I am an auto-discovered task in crates/aspect-cli/.aspect/user-task.axl")
    return 0

user_task = task(
    name = "user-task",
    group = ["user", "sub"],
    implementation = _impl,
    args = {},
)