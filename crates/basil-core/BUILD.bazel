load("//bazel/rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "basil-core",
    srcs = glob(["src/**/*.rs"]),
    visibility = [
        "//crates/axl-runtime:__pkg__",
        "//crates/basil:__pkg__",
    ],
    deps = [
        "//crates/axl-proto",
        "@crates//:prost",
    ],
)

rust_test(
    name = "test",
    crate = ":basil-core",
)
