load("//bazel/rust:defs.bzl", "rust_library")

rust_library(
    name = "build-event-stream",
    srcs = glob(["src/**/*.rs"]),
    deps = [
        "@crates//:futures",
        "@crates//:http",
        "@crates//:prost-types",
        "@crates//:thiserror",
        "@crates//:tonic",
        "//crates/axl-proto",
    ],
    visibility = [
        "//crates/axl-runtime:__pkg__",
    ],
)
