load("//bazel/rust:defs.bzl", "rust_library")

rust_library(
    name = "build-event-stream",
    srcs = glob(["src/**/*.rs"]),
    visibility = [
        "//crates/axl-runtime:__pkg__",
    ],
    deps = [
        "//crates/axl-proto",
        "@crates//:futures",
        "@crates//:http",
        "@crates//:prost-types",
        "@crates//:thiserror",
        "@crates//:tonic",
    ],
)
