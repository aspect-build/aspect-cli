// Import of parent project directories

export { subproject_lib as backref_subproject_lib } from '../subproject/lib';

// Also depending on the index.ts file
export { subproject_index as backref_subproject_index } from '../subproject';
