// Comments in JSON
{
    /* other comment syntax */

    	"//": "inner comment (with tab indent)",   
  	"//": "duplicate keys",		

    // Nested comment
    "compilerOptions": {
      "rootDir": "src", // trailing commas
    },
}